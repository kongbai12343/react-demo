import { styled } from "styled-components";
import { Layout } from "antd";

const { Sider } = Layout;

export const SiderBoxWrapper = styled(Sider)`
  height: 100vh;
  overflow: hidden;
  overflow-y: scroll;
`;